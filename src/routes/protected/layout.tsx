// src/routes/protected/layout.tsx
// Este archivo actúa como el layout para todas las sub-rutas bajo /protected
import ProtectedRouteLayoutContent from '@/features/authFeature/routes/ProtectedRouteLayout'

export default function ProtectedLayout() {
  return <ProtectedRouteLayoutContent />
}
